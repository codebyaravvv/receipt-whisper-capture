
# Core dependencies
numpy>=1.20.0
pillow>=9.0.0
scikit-learn>=1.0.2
matplotlib>=3.5.1
opencv-python>=4.5.5

# Web framework
flask>=2.0.2
flask-cors>=3.0.10
python-dotenv>=0.19.2

# TensorFlow - try multiple options based on platform
# For Apple Silicon (M1/M2/M3) Macs
tensorflow-macos>=2.8.0,<2.12.0; sys_platform == 'darwin' and platform_machine == 'arm64'

# Standard TensorFlow for other platforms
tensorflow>=2.8.0,<2.12.0; sys_platform != 'darwin' or platform_machine != 'arm64'
